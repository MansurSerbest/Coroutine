The custom coroutine class' header file (Coroutine.h) can be found in the following directory: CustomCoroutine_Inc_Lib\inc
==================================================================================================================================

CustomCoroProfiler: contains the project which profiles the custom coroutine

CustomCoroutine_Inc_Lib: contains the necessary header files and lib files which need to be
 included inside a project to use the custom coroutine

ResultProject: the project which uses Coroutine.h to use it in a test case